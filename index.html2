<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Eyewa Talent OS</title>
<link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Outfit:wght@300;400;500;600;700&display=swap" rel="stylesheet">
<style>
:root {
  --bg: #0b0c10;
  --surface: #13141a;
  --card: #1a1c24;
  --border: #252830;
  --border2: #2e3140;
  --accent: #e8c547;
  --accent2: #4f8ef7;
  --green: #3ecf8e;
  --red: #f75c5c;
  --orange: #f7923a;
  --purple: #a855f7;
  --text: #eeeef0;
  --mid: #9395a0;
  --muted: #5a5c6a;
  --display: 'Bebas Neue', sans-serif;
  --body: 'Outfit', sans-serif;
  --sidebar-w: 220px;
}
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
body { background: var(--bg); color: var(--text); font-family: var(--body); font-size: 14px; min-height: 100vh; display: flex; overflow: hidden; }

/* ── SIDEBAR ── */
.sidebar {
  width: var(--sidebar-w);
  background: var(--surface);
  border-right: 1px solid var(--border);
  display: flex;
  flex-direction: column;
  height: 100vh;
  flex-shrink: 0;
  position: fixed;
  left: 0; top: 0;
  z-index: 100;
}
.sidebar-brand {
  padding: 22px 20px 18px;
  border-bottom: 1px solid var(--border);
  display: flex;
  align-items: center;
  gap: 10px;
}
.brand-logo {
  font-family: var(--display);
  font-size: 28px;
  letter-spacing: 0.04em;
  color: var(--text);
  line-height: 1;
}
.brand-logo span { color: var(--accent); }
.brand-tag {
  font-size: 9px;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--muted);
  margin-top: 2px;
}
.sidebar-section { padding: 16px 12px 8px; }
.sidebar-section-label {
  font-size: 9px;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: var(--muted);
  padding: 0 8px;
  margin-bottom: 6px;
  font-weight: 600;
}
.nav-item {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 9px 10px;
  border-radius: 7px;
  cursor: pointer;
  transition: all 0.15s;
  color: var(--mid);
  font-size: 13.5px;
  font-weight: 500;
  margin-bottom: 2px;
  border: 1px solid transparent;
  position: relative;
}
.nav-item:hover { background: var(--card); color: var(--text); }
.nav-item.active { background: rgba(232,197,71,0.1); color: var(--accent); border-color: rgba(232,197,71,0.15); }
.nav-icon { font-size: 15px; width: 20px; text-align: center; flex-shrink: 0; }
.nav-badge {
  margin-left: auto;
  background: var(--accent);
  color: var(--bg);
  font-size: 9px;
  font-weight: 700;
  padding: 2px 6px;
  border-radius: 10px;
  min-width: 18px;
  text-align: center;
}
.sidebar-bottom {
  margin-top: auto;
  padding: 16px;
  border-top: 1px solid var(--border);
}
.user-chip {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 10px 12px;
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: 8px;
}
.user-avatar {
  width: 30px; height: 30px;
  border-radius: 50%;
  background: linear-gradient(135deg, var(--accent), var(--orange));
  display: flex; align-items: center; justify-content: center;
  font-size: 12px;
  font-weight: 700;
  color: var(--bg);
  flex-shrink: 0;
}
.user-name { font-size: 12px; font-weight: 600; }
.user-role { font-size: 10px; color: var(--muted); }

/* ── MAIN CONTENT ── */
.content {
  margin-left: var(--sidebar-w);
  flex: 1;
  height: 100vh;
  overflow-y: auto;
  position: relative;
}

/* ── TOP BAR ── */
.topbar {
  background: var(--surface);
  border-bottom: 1px solid var(--border);
  padding: 0 32px;
  height: 56px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  position: sticky;
  top: 0;
  z-index: 50;
}
.topbar-title {
  font-family: var(--display);
  font-size: 22px;
  letter-spacing: 0.06em;
  color: var(--text);
}
.topbar-actions { display: flex; gap: 10px; align-items: center; }
.btn {
  font-family: var(--body);
  font-size: 12px;
  font-weight: 600;
  padding: 8px 16px;
  border-radius: 6px;
  border: none;
  cursor: pointer;
  transition: all 0.15s;
  letter-spacing: 0.02em;
}
.btn-primary { background: var(--accent); color: var(--bg); }
.btn-primary:hover { background: #d4b03a; transform: translateY(-1px); }
.btn-ghost { background: transparent; color: var(--mid); border: 1px solid var(--border2); }
.btn-ghost:hover { border-color: var(--text); color: var(--text); }
.btn-danger { background: rgba(247,92,92,0.12); color: var(--red); border: 1px solid rgba(247,92,92,0.2); }
.btn-danger:hover { background: rgba(247,92,92,0.2); }
.btn-green { background: rgba(62,207,142,0.12); color: var(--green); border: 1px solid rgba(62,207,142,0.2); }
.btn-green:hover { background: rgba(62,207,142,0.2); }
.btn-sm { padding: 5px 12px; font-size: 11px; }

/* ── PAGE ── */
.page { display: none; padding: 28px 32px; animation: fadeIn 0.25s ease; }
.page.active { display: block; }
@keyframes fadeIn { from{opacity:0;transform:translateY(8px)} to{opacity:1;transform:translateY(0)} }

/* ── DASHBOARD ── */
.stats-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 16px; margin-bottom: 28px; }
.stat-card {
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: 10px;
  padding: 20px;
  position: relative;
  overflow: hidden;
}
.stat-card::before {
  content: '';
  position: absolute;
  top: 0; left: 0; right: 0;
  height: 2px;
}
.stat-card.yellow::before { background: var(--accent); }
.stat-card.blue::before { background: var(--accent2); }
.stat-card.green::before { background: var(--green); }
.stat-card.red::before { background: var(--red); }
.stat-label { font-size: 10px; letter-spacing: 0.12em; text-transform: uppercase; color: var(--muted); margin-bottom: 10px; font-weight: 600; }
.stat-num { font-family: var(--display); font-size: 42px; letter-spacing: 0.04em; line-height: 1; margin-bottom: 4px; }
.stat-card.yellow .stat-num { color: var(--accent); }
.stat-card.blue .stat-num { color: var(--accent2); }
.stat-card.green .stat-num { color: var(--green); }
.stat-card.red .stat-num { color: var(--red); }
.stat-sub { font-size: 11px; color: var(--muted); }

.dash-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; }
.dash-card {
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: 10px;
  overflow: hidden;
}
.dash-card-header {
  padding: 16px 20px;
  border-bottom: 1px solid var(--border);
  display: flex;
  align-items: center;
  justify-content: space-between;
}
.dash-card-title { font-size: 13px; font-weight: 600; color: var(--text); }
.dash-card-body { padding: 16px 20px; }

/* ── TABLES ── */
.table-wrap { background: var(--card); border: 1px solid var(--border); border-radius: 10px; overflow: hidden; }
.table-header {
  padding: 14px 20px;
  border-bottom: 1px solid var(--border);
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
}
.table-title { font-size: 14px; font-weight: 600; }
.search-input {
  background: var(--surface);
  border: 1px solid var(--border2);
  border-radius: 6px;
  padding: 7px 12px;
  font-family: var(--body);
  font-size: 12px;
  color: var(--text);
  outline: none;
  width: 220px;
  transition: border-color 0.15s;
}
.search-input:focus { border-color: var(--accent); }
.search-input::placeholder { color: var(--muted); }

table { width: 100%; border-collapse: collapse; }
thead th {
  padding: 10px 16px;
  font-size: 10px;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: var(--muted);
  text-align: left;
  border-bottom: 1px solid var(--border);
  font-weight: 600;
  background: var(--surface);
}
tbody td { padding: 13px 16px; border-bottom: 1px solid var(--border); font-size: 13px; vertical-align: middle; }
tbody tr:last-child td { border-bottom: none; }
tbody tr { transition: background 0.12s; cursor: pointer; }
tbody tr:hover td { background: rgba(255,255,255,0.02); }

/* ── BADGES ── */
.badge {
  display: inline-flex;
  align-items: center;
  padding: 3px 10px;
  border-radius: 20px;
  font-size: 10px;
  font-weight: 600;
  letter-spacing: 0.06em;
  white-space: nowrap;
}
.badge-open { background: rgba(62,207,142,0.12); color: var(--green); border: 1px solid rgba(62,207,142,0.2); }
.badge-draft { background: rgba(147,149,160,0.12); color: var(--mid); border: 1px solid var(--border2); }
.badge-hold { background: rgba(247,146,58,0.12); color: var(--orange); border: 1px solid rgba(247,146,58,0.2); }
.badge-closed { background: rgba(247,92,92,0.12); color: var(--red); border: 1px solid rgba(247,92,92,0.2); }
.badge-active { background: rgba(79,142,247,0.12); color: var(--accent2); border: 1px solid rgba(79,142,247,0.2); }
.badge-hired { background: rgba(62,207,142,0.12); color: var(--green); border: 1px solid rgba(62,207,142,0.2); }
.badge-rejected { background: rgba(247,92,92,0.12); color: var(--red); border: 1px solid rgba(247,92,92,0.2); }
.badge-offer { background: rgba(168,85,247,0.12); color: var(--purple); border: 1px solid rgba(168,85,247,0.2); }
.badge-screen { background: rgba(232,197,71,0.12); color: var(--accent); border: 1px solid rgba(232,197,71,0.2); }

/* ── STAGE PILL ── */
.stage-pill { display: inline-flex; align-items: center; gap: 6px; padding: 4px 10px; border-radius: 20px; font-size: 11px; font-weight: 500; }

/* ── PRIORITY ── */
.priority-dot { width: 7px; height: 7px; border-radius: 50%; display: inline-block; margin-right: 6px; }
.p-critical { background: var(--red); }
.p-high { background: var(--orange); }
.p-normal { background: var(--green); }

/* ── MODAL ── */
.modal-overlay {
  display: none;
  position: fixed;
  inset: 0;
  background: rgba(11,12,16,0.85);
  z-index: 500;
  align-items: flex-start;
  justify-content: center;
  padding: 32px 20px;
  overflow-y: auto;
}
.modal-overlay.open { display: flex; }
.modal {
  background: var(--surface);
  border: 1px solid var(--border2);
  border-radius: 14px;
  width: 100%;
  max-width: 620px;
  margin: auto;
  animation: modalIn 0.2s ease;
  overflow: hidden;
}
.modal-lg { max-width: 820px; }
@keyframes modalIn { from{opacity:0;transform:translateY(-16px)} to{opacity:1;transform:translateY(0)} }
.modal-header {
  padding: 20px 24px;
  border-bottom: 1px solid var(--border);
  display: flex;
  align-items: center;
  justify-content: space-between;
}
.modal-title { font-size: 16px; font-weight: 700; }
.modal-close {
  background: var(--card);
  border: 1px solid var(--border);
  color: var(--mid);
  width: 30px; height: 30px;
  border-radius: 6px;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 16px;
  transition: all 0.15s;
}
.modal-close:hover { color: var(--text); border-color: var(--border2); }
.modal-body { padding: 24px; }
.modal-footer {
  padding: 16px 24px;
  border-top: 1px solid var(--border);
  display: flex;
  justify-content: flex-end;
  gap: 10px;
}

/* ── FORMS ── */
.form-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
.form-grid-3 { display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 16px; }
.field { display: flex; flex-direction: column; gap: 6px; }
.field.full { grid-column: 1 / -1; }
.field-label {
  font-size: 10px;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: var(--muted);
  font-weight: 600;
}
.req { color: var(--accent); margin-left: 2px; }
input[type="text"], input[type="email"], input[type="tel"], select, textarea {
  background: var(--card);
  border: 1px solid var(--border2);
  border-radius: 6px;
  padding: 9px 12px;
  font-family: var(--body);
  font-size: 13px;
  color: var(--text);
  outline: none;
  width: 100%;
  transition: border-color 0.15s, box-shadow 0.15s;
  appearance: none;
}
input:focus, select:focus, textarea:focus {
  border-color: var(--accent);
  box-shadow: 0 0 0 3px rgba(232,197,71,0.1);
}
textarea { resize: vertical; min-height: 90px; line-height: 1.6; }
select option { background: var(--card); }

/* ── DETAIL VIEW ── */
.detail-header {
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: 24px 28px;
  margin-bottom: 20px;
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 20px;
}
.detail-back {
  font-size: 12px;
  color: var(--muted);
  cursor: pointer;
  display: flex;
  align-items: center;
  gap: 6px;
  margin-bottom: 12px;
  transition: color 0.15s;
}
.detail-back:hover { color: var(--text); }
.detail-name { font-family: var(--display); font-size: 32px; letter-spacing: 0.04em; margin-bottom: 8px; }
.detail-meta { display: flex; gap: 12px; align-items: center; flex-wrap: wrap; font-size: 12px; color: var(--mid); }

.tabs { display: flex; gap: 0; border-bottom: 1px solid var(--border); margin-bottom: 24px; }
.tab {
  padding: 10px 20px;
  font-size: 13px;
  font-weight: 500;
  color: var(--muted);
  cursor: pointer;
  border-bottom: 2px solid transparent;
  transition: all 0.15s;
  margin-bottom: -1px;
}
.tab:hover { color: var(--text); }
.tab.active { color: var(--accent); border-bottom-color: var(--accent); }

.tab-panel { display: none; }
.tab-panel.active { display: block; animation: fadeIn 0.2s ease; }

/* ── KANBAN ── */
.kanban { display: flex; gap: 14px; overflow-x: auto; padding-bottom: 16px; }
.kanban-col {
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: 10px;
  min-width: 230px;
  max-width: 230px;
  flex-shrink: 0;
  overflow: hidden;
}
.kanban-col-header {
  padding: 12px 14px;
  border-bottom: 1px solid var(--border);
  display: flex;
  align-items: center;
  justify-content: space-between;
}
.kanban-col-name { font-size: 12px; font-weight: 600; }
.kanban-col-count {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 10px;
  font-size: 10px;
  font-weight: 700;
  padding: 1px 8px;
  color: var(--mid);
}
.kanban-cards { padding: 10px; display: flex; flex-direction: column; gap: 8px; min-height: 80px; }
.kanban-card {
  background: var(--surface);
  border: 1px solid var(--border2);
  border-radius: 8px;
  padding: 12px;
  cursor: pointer;
  transition: all 0.15s;
}
.kanban-card:hover { border-color: var(--accent); transform: translateY(-1px); }
.kc-name { font-size: 13px; font-weight: 600; margin-bottom: 4px; }
.kc-role { font-size: 11px; color: var(--muted); margin-bottom: 8px; }
.kc-meta { display: flex; justify-content: space-between; align-items: center; }
.kc-days { font-size: 10px; color: var(--muted); }

/* ── ACTIVITY FEED ── */
.activity-item {
  display: flex;
  gap: 12px;
  padding: 12px 0;
  border-bottom: 1px solid var(--border);
  font-size: 12px;
}
.activity-item:last-child { border-bottom: none; }
.activity-dot { width: 8px; height: 8px; border-radius: 50%; background: var(--accent); flex-shrink: 0; margin-top: 4px; }
.activity-text { color: var(--mid); line-height: 1.5; }
.activity-text strong { color: var(--text); }
.activity-time { font-size: 10px; color: var(--muted); margin-top: 2px; }

/* ── COMMS LOG ── */
.comms-item {
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: 8px;
  padding: 14px 16px;
  margin-bottom: 10px;
}
.comms-item-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 6px; }
.comms-type { font-size: 11px; font-weight: 600; color: var(--accent2); }
.comms-date { font-size: 10px; color: var(--muted); }
.comms-subject { font-size: 13px; font-weight: 600; margin-bottom: 4px; }
.comms-preview { font-size: 12px; color: var(--mid); line-height: 1.5; }

/* ── SCORECARD DISPLAY ── */
.scorecard-row {
  display: grid;
  grid-template-columns: 2fr 1fr 2fr;
  gap: 12px;
  padding: 12px 0;
  border-bottom: 1px solid var(--border);
  align-items: start;
  font-size: 13px;
}
.scorecard-row:last-child { border-bottom: none; }
.score-display { display: flex; gap: 4px; }
.score-pip { width: 28px; height: 28px; border-radius: 4px; border: 1px solid var(--border2); display: flex; align-items: center; justify-content: center; font-size: 11px; font-weight: 600; color: var(--muted); transition: all 0.15s; }
.score-pip.filled-1, .score-pip.filled-2 { background: rgba(247,92,92,0.2); color: var(--red); border-color: rgba(247,92,92,0.3); }
.score-pip.filled-3 { background: rgba(247,146,58,0.2); color: var(--orange); border-color: rgba(247,146,58,0.3); }
.score-pip.filled-4 { background: rgba(62,207,142,0.15); color: var(--green); border-color: rgba(62,207,142,0.3); }
.score-pip.filled-5 { background: rgba(62,207,142,0.25); color: var(--green); border-color: rgba(62,207,142,0.4); }

/* ── LOADING ── */
.loading-overlay {
  display: none;
  position: fixed;
  inset: 0;
  background: rgba(11,12,16,0.9);
  z-index: 1000;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 16px;
}
.loading-overlay.on { display: flex; }
.loading-spinner {
  width: 40px; height: 40px;
  border: 2px solid var(--border2);
  border-top-color: var(--accent);
  border-radius: 50%;
  animation: spin 0.7s linear infinite;
}
@keyframes spin { to { transform: rotate(360deg); } }
.loading-text { font-family: var(--display); font-size: 20px; letter-spacing: 0.08em; color: var(--text); }
.loading-sub { font-size: 12px; color: var(--muted); text-align: center; max-width: 280px; line-height: 1.7; }

/* ── EMPTY STATE ── */
.empty { padding: 48px; text-align: center; color: var(--muted); }
.empty-icon { font-size: 36px; margin-bottom: 12px; opacity: 0.5; }
.empty-title { font-size: 15px; font-weight: 600; color: var(--mid); margin-bottom: 6px; }
.empty-sub { font-size: 12px; line-height: 1.7; max-width: 300px; margin: 0 auto; }

/* ── TOAST ── */
.toast {
  position: fixed;
  bottom: 24px;
  right: 24px;
  background: var(--card);
  border: 1px solid var(--border2);
  border-left: 3px solid var(--green);
  border-radius: 8px;
  padding: 12px 18px;
  font-size: 13px;
  font-weight: 500;
  z-index: 2000;
  opacity: 0;
  transform: translateY(8px);
  transition: all 0.25s;
  pointer-events: none;
}
.toast.show { opacity: 1; transform: translateY(0); }
.toast.error { border-left-color: var(--red); }

/* ── REFERENCE CHECK ── */
.ref-card {
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: 8px;
  padding: 16px;
  margin-bottom: 12px;
}
.ref-card-title { font-size: 13px; font-weight: 600; margin-bottom: 4px; }
.ref-card-sub { font-size: 11px; color: var(--muted); }

/* ── GEN OUTPUT ── */
.gen-output {
  background: var(--card);
  border: 1px solid var(--border2);
  border-radius: 8px;
  padding: 18px;
  margin-top: 14px;
  font-size: 13px;
  line-height: 1.75;
  color: var(--text);
  white-space: pre-wrap;
  max-height: 400px;
  overflow-y: auto;
}
.gen-actions { display: flex; gap: 8px; margin-top: 10px; }

/* ── MISC ── */
.divider { height: 1px; background: var(--border); margin: 20px 0; }
.section-title { font-size: 13px; font-weight: 700; color: var(--text); margin-bottom: 14px; display: flex; align-items: center; gap: 8px; }
.section-title::after { content: ''; flex: 1; height: 1px; background: var(--border); }
.info-row { display: flex; gap: 8px; margin-bottom: 8px; font-size: 13px; }
.info-key { color: var(--muted); min-width: 120px; flex-shrink: 0; }
.info-val { color: var(--text); font-weight: 500; }
.text-muted { color: var(--muted); }
.text-sm { font-size: 11px; }
.mt-12 { margin-top: 12px; }
.mt-20 { margin-top: 20px; }
.gap-8 { gap: 8px; }
.flex { display: flex; }
.flex-col { flex-direction: column; }
.items-center { align-items: center; }
.justify-between { justify-content: space-between; }
.wrap { flex-wrap: wrap; }

@media (max-width: 900px) {
  .sidebar { display: none; }
  .content { margin-left: 0; }
  .stats-grid { grid-template-columns: 1fr 1fr; }
  .dash-grid { grid-template-columns: 1fr; }
}
</style>
</head>
<body>

<!-- ── SIDEBAR ── -->
<div class="sidebar">
  <div class="sidebar-brand">
    <div>
      <div class="brand-logo">Eyewa<span>.</span></div>
      <div class="brand-tag">Talent OS</div>
    </div>
  </div>
  <div class="sidebar-section">
    <div class="sidebar-section-label">Main</div>
    <div class="nav-item active" onclick="nav('dashboard')"><span class="nav-icon">◈</span> Dashboard</div>
    <div class="nav-item" onclick="nav('pipeline')"><span class="nav-icon">⬡</span> Pipeline <span class="nav-badge" id="nb-pipeline">0</span></div>
    <div class="nav-item" onclick="nav('roles')"><span class="nav-icon">◻</span> Roles <span class="nav-badge" id="nb-roles">0</span></div>
    <div class="nav-item" onclick="nav('candidates')"><span class="nav-icon">◯</span> Candidates <span class="nav-badge" id="nb-candidates">0</span></div>
  </div>
  <div class="sidebar-section">
    <div class="sidebar-section-label">Tools</div>
    <div class="nav-item" onclick="nav('tool-jd')"><span class="nav-icon">✦</span> JD Generator</div>
    <div class="nav-item" onclick="nav('tool-interview')"><span class="nav-icon">◈</span> Interview Pack</div>
    <div class="nav-item" onclick="nav('tool-comms')"><span class="nav-icon">✉</span> Candidate Comms</div>
    <div class="nav-item" onclick="nav('tool-scorecard')"><span class="nav-icon">◉</span> Scorecards</div>
    <div class="nav-item" onclick="nav('tool-refcheck')"><span class="nav-icon">◎</span> Reference Check</div>
  </div>
  <div class="sidebar-bottom">
    <div class="user-chip">
      <div class="user-avatar">HP</div>
      <div>
        <div class="user-name">Humairaa</div>
        <div class="user-role">TA Manager</div>
      </div>
    </div>
  </div>
</div>

<!-- ── CONTENT ── -->
<div class="content">

  <!-- TOPBAR -->
  <div class="topbar">
    <div class="topbar-title" id="topbar-title">DASHBOARD</div>
    <div class="topbar-actions" id="topbar-actions">
      <button class="btn btn-ghost btn-sm" onclick="openModal('modal-add-role')">+ New Role</button>
      <button class="btn btn-primary btn-sm" onclick="openModal('modal-add-candidate')">+ Add Candidate</button>
    </div>
  </div>

  <!-- ── DASHBOARD ── -->
  <div class="page active" id="page-dashboard">
    <div class="stats-grid">
      <div class="stat-card yellow">
        <div class="stat-label">Open Roles</div>
        <div class="stat-num" id="stat-open">0</div>
        <div class="stat-sub">Active requisitions</div>
      </div>
      <div class="stat-card blue">
        <div class="stat-label">Active Candidates</div>
        <div class="stat-num" id="stat-candidates">0</div>
        <div class="stat-sub">In process</div>
      </div>
      <div class="stat-card green">
        <div class="stat-label">Hired This Month</div>
        <div class="stat-num" id="stat-hired">0</div>
        <div class="stat-sub">Offers accepted</div>
      </div>
      <div class="stat-card red">
        <div class="stat-label">Pending Feedback</div>
        <div class="stat-num" id="stat-feedback">0</div>
        <div class="stat-sub">Scorecards missing</div>
      </div>
    </div>
    <div class="dash-grid">
      <div class="dash-card">
        <div class="dash-card-header">
          <div class="dash-card-title">Open Roles</div>
          <button class="btn btn-ghost btn-sm" onclick="openModal('modal-add-role')">+ New</button>
        </div>
        <div class="dash-card-body" id="dash-roles-list">
          <div class="empty"><div class="empty-icon">◻</div><div class="empty-title">No roles yet</div><div class="empty-sub">Create a role to get started</div></div>
        </div>
      </div>
      <div class="dash-card">
        <div class="dash-card-header">
          <div class="dash-card-title">Recent Activity</div>
        </div>
        <div class="dash-card-body" id="activity-feed">
          <div class="empty"><div class="empty-icon">◈</div><div class="empty-title">No activity yet</div></div>
        </div>
      </div>
    </div>
  </div>

  <!-- ── PIPELINE ── -->
  <div class="page" id="page-pipeline">
    <div class="kanban" id="kanban-board"></div>
  </div>

  <!-- ── ROLES ── -->
  <div class="page" id="page-roles">
    <div class="table-wrap">
      <div class="table-header">
        <div class="table-title">All Roles</div>
        <div class="flex gap-8 items-center">
          <input class="search-input" placeholder="Search roles..." oninput="filterTable('roles-table', this.value)">
          <button class="btn btn-primary btn-sm" onclick="openModal('modal-add-role')">+ New Role</button>
        </div>
      </div>
      <table id="roles-table">
        <thead><tr>
          <th>Role</th><th>Department</th><th>Priority</th><th>Status</th><th>Candidates</th><th>Opened</th><th></th>
        </tr></thead>
        <tbody id="roles-tbody"></tbody>
      </table>
      <div class="empty" id="roles-empty" style="display:none">
        <div class="empty-icon">◻</div>
        <div class="empty-title">No roles yet</div>
        <div class="empty-sub">Add your first role to start building your pipeline</div>
      </div>
    </div>
  </div>

  <!-- ── ROLE DETAIL ── -->
  <div class="page" id="page-role-detail">
    <div class="detail-back" onclick="nav('roles')">← Back to Roles</div>
    <div class="detail-header">
      <div>
        <div class="detail-name" id="rd-title"></div>
        <div class="detail-meta" id="rd-meta"></div>
      </div>
      <div class="flex gap-8 wrap">
        <button class="btn btn-ghost btn-sm" onclick="openAddCandidateToRole()">+ Add Candidate</button>
        <button class="btn btn-primary btn-sm" onclick="openRoleTools()">Generate Tools</button>
      </div>
    </div>
    <div class="tabs">
      <div class="tab active" onclick="switchTab('role', 'overview', this)">Overview</div>
      <div class="tab" onclick="switchTab('role', 'candidates', this)">Candidates</div>
      <div class="tab" onclick="switchTab('role', 'jd', this)">Job Description</div>
      <div class="tab" onclick="switchTab('role', 'interview', this)">Interview Pack</div>
    </div>
    <div class="tab-panel active" id="role-tab-overview">
      <div class="form-grid" style="gap:12px" id="rd-overview-grid"></div>
    </div>
    <div class="tab-panel" id="role-tab-candidates">
      <div id="rd-candidates-list"></div>
    </div>
    <div class="tab-panel" id="role-tab-jd">
      <div id="rd-jd-content"></div>
    </div>
    <div class="tab-panel" id="role-tab-interview">
      <div id="rd-interview-content"></div>
    </div>
  </div>

  <!-- ── CANDIDATES ── -->
  <div class="page" id="page-candidates">
    <div class="table-wrap">
      <div class="table-header">
        <div class="table-title">All Candidates</div>
        <div class="flex gap-8 items-center">
          <input class="search-input" placeholder="Search candidates..." oninput="filterTable('candidates-table', this.value)">
          <button class="btn btn-primary btn-sm" onclick="openModal('modal-add-candidate')">+ Add Candidate</button>
        </div>
      </div>
      <table id="candidates-table">
        <thead><tr>
          <th>Name</th><th>Current Role</th><th>Source</th><th>Applied To</th><th>Stage</th><th>Added</th><th></th>
        </tr></thead>
        <tbody id="candidates-tbody"></tbody>
      </table>
      <div class="empty" id="candidates-empty" style="display:none">
        <div class="empty-icon">◯</div>
        <div class="empty-title">No candidates yet</div>
        <div class="empty-sub">Add your first candidate and tag them to a role</div>
      </div>
    </div>
  </div>

  <!-- ── CANDIDATE DETAIL ── -->
  <div class="page" id="page-candidate-detail">
    <div class="detail-back" onclick="nav('candidates')">← Back to Candidates</div>
    <div class="detail-header">
      <div>
        <div class="detail-name" id="cd-name"></div>
        <div class="detail-meta" id="cd-meta"></div>
      </div>
      <div class="flex gap-8 wrap">
        <button class="btn btn-ghost btn-sm" onclick="openCandidateComms()">✉ Send Comms</button>
        <button class="btn btn-ghost btn-sm" onclick="openScorecard()">◉ Scorecard</button>
        <button class="btn btn-primary btn-sm" onclick="openRefCheck()">◎ Ref Check</button>
      </div>
    </div>
    <div class="tabs">
      <div class="tab active" onclick="switchTab('candidate', 'overview', this)">Overview</div>
      <div class="tab" onclick="switchTab('candidate', 'comms', this)">Comms</div>
      <div class="tab" onclick="switchTab('candidate', 'scorecards', this)">Scorecards</div>
      <div class="tab" onclick="switchTab('candidate', 'refcheck', this)">Reference Check</div>
    </div>
    <div class="tab-panel active" id="candidate-tab-overview">
      <div class="form-grid" id="cd-overview-grid"></div>
    </div>
    <div class="tab-panel" id="candidate-tab-comms">
      <div id="cd-comms-list"></div>
    </div>
    <div class="tab-panel" id="candidate-tab-scorecards">
      <div id="cd-scorecards-list"></div>
    </div>
    <div class="tab-panel" id="candidate-tab-refcheck">
      <div id="cd-refcheck-content"></div>
    </div>
  </div>

  <!-- ── TOOL PAGES (simplified embedded versions) ── -->
  <div class="page" id="page-tool-jd">
    <div class="table-wrap" style="padding:24px">
      <div class="section-title">JD Generator</div>
      <div class="form-grid">
        <div class="field"><label class="field-label">Job Title <span class="req">*</span></label><input type="text" id="t-jd-title" placeholder="e.g. Marketing Manager"></div>
        <div class="field"><label class="field-label">Department</label><input type="text" id="t-jd-dept" placeholder="e.g. Marketing"></div>
        <div class="field"><label class="field-label">Reports To</label><input type="text" id="t-jd-reports" placeholder="e.g. CMO"></div>
        <div class="field"><label class="field-label">Seniority</label>
          <select id="t-jd-level"><option>Mid-level</option><option>Senior</option><option>Manager</option><option>Head of</option><option>Director</option></select>
        </div>
        <div class="field full"><label class="field-label">Key Responsibilities <span class="req">*</span></label><textarea id="t-jd-resp" placeholder="What will this person own?"></textarea></div>
        <div class="field full"><label class="field-label">Requirements <span class="req">*</span></label><textarea id="t-jd-req" placeholder="What are we looking for?"></textarea></div>
        <div class="field full"><label class="field-label">Tone</label>
          <select id="t-jd-tone"><option value="energetic and direct">Energetic & Direct (Eyewa default)</option><option value="professional and formal">Professional</option><option value="warm and human">Warm & Human</option><option value="bold and ambitious">Bold & Ambitious</option></select>
        </div>
      </div>
      <div class="flex gap-8 mt-20">
        <button class="btn btn-primary" onclick="runJDGenerator()">Generate JD →</button>
        <button class="btn btn-ghost" onclick="clearTool('jd')">Clear</button>
      </div>
      <div id="jd-tool-output" style="display:none">
        <div class="gen-output" id="jd-output-text"></div>
        <div class="gen-actions">
          <button class="btn btn-ghost btn-sm" onclick="copyGen('jd-output-text')">Copy</button>
          <button class="btn btn-ghost btn-sm" onclick="window.print()">Print / PDF</button>
        </div>
      </div>
    </div>
  </div>

  <div class="page" id="page-tool-interview">
    <div class="table-wrap" style="padding:24px">
      <div class="section-title">Interview Pack Generator</div>
      <div class="form-grid">
        <div class="field"><label class="field-label">Role Title <span class="req">*</span></label><input type="text" id="t-ip-title" placeholder="e.g. Marketing Manager"></div>
        <div class="field"><label class="field-label">Seniority</label>
          <select id="t-ip-level"><option>Mid-level</option><option>Senior</option><option>Manager</option><option>Head of</option><option>Director</option></select>
        </div>
        <div class="field full"><label class="field-label">Job Description / Role Context <span class="req">*</span></label><textarea id="t-ip-jd" placeholder="Paste the JD or describe the role"></textarea></div>
        <div class="field full"><label class="field-label">Stages (comma separated)</label><input type="text" id="t-ip-stages" placeholder="Recruiter Screen, HM Interview, CXO Interview" value="Recruiter Screen, HM Interview"></div>
        <div class="field full"><label class="field-label">Competencies to focus on</label><input type="text" id="t-ip-comps" placeholder="e.g. Commercial acumen, Leadership, Data analysis"></div>
      </div>
      <div class="flex gap-8 mt-20">
        <button class="btn btn-primary" onclick="runInterviewPack()">Generate Pack →</button>
        <button class="btn btn-ghost" onclick="clearTool('interview')">Clear</button>
      </div>
      <div id="interview-tool-output" style="display:none">
        <div class="gen-output" id="interview-output-text"></div>
        <div class="gen-actions">
          <button class="btn btn-ghost btn-sm" onclick="copyGen('interview-output-text')">Copy</button>
        </div>
      </div>
    </div>
  </div>

  <div class="page" id="page-tool-comms">
    <div class="table-wrap" style="padding:24px">
      <div class="section-title">Candidate Comms</div>
      <div class="form-grid">
        <div class="field"><label class="field-label">Candidate Name <span class="req">*</span></label><input type="text" id="t-cm-name" placeholder="e.g. Sara"></div>
        <div class="field"><label class="field-label">Candidate Email</label><input type="email" id="t-cm-email" placeholder="sara@email.com"></div>
        <div class="field"><label class="field-label">Role Title <span class="req">*</span></label><input type="text" id="t-cm-role" placeholder="e.g. Marketing Manager"></div>
        <div class="field"><label class="field-label">Email Type <span class="req">*</span></label>
          <select id="t-cm-type">
            <option value="interview confirmation">Interview Confirmation</option>
            <option value="post-interview thank you with 5 working day update promise">Post-Interview Thank You</option>
            <option value="application rejection (no interview)">Application Rejection</option>
            <option value="post-interview rejection">Post-Interview Rejection</option>
            <option value="verbal offer">Offer Email</option>
            <option value="general process update">General Update</option>
            <option value="reference check request to candidate">Reference Check Request (to Candidate)</option>
          </select>
        </div>
        <div class="field"><label class="field-label">Your Name</label><input type="text" id="t-cm-sender" placeholder="Humairaa" value="Humairaa"></div>
        <div class="field"><label class="field-label">Your Email</label><input type="email" id="t-cm-senderemail" placeholder="humairaa.patel@eyewa.com" value="humairaa.patel@eyewa.com"></div>
        <div class="field full"><label class="field-label">Additional Context</label><textarea id="t-cm-context" style="min-height:70px" placeholder="e.g. Interview on Tuesday 13 May at 2pm via Google Meet. Interviewer: Rami."></textarea></div>
      </div>
      <div class="flex gap-8 mt-20">
        <button class="btn btn-primary" onclick="runCommsGenerator()">Generate Email →</button>
        <button class="btn btn-ghost" onclick="clearTool('comms')">Clear</button>
      </div>
      <div id="comms-tool-output" style="display:none">
        <div style="background:var(--card);border:1px solid var(--border2);border-radius:8px;padding:14px 16px;margin-top:14px;">
          <div style="font-size:11px;color:var(--muted);margin-bottom:4px">SUBJECT</div>
          <div id="comms-output-subject" style="font-weight:600;font-size:14px;margin-bottom:12px"></div>
          <div class="gen-output" id="comms-output-body" style="margin-top:0;max-height:300px"></div>
        </div>
        <div class="gen-actions">
          <button class="btn btn-primary btn-sm" onclick="openMailFromComms()">✉ Open in Mail</button>
          <button class="btn btn-ghost btn-sm" onclick="copyGen('comms-output-body')">Copy Body</button>
        </div>
      </div>
    </div>
  </div>

  <div class="page" id="page-tool-scorecard">
    <div class="table-wrap" style="padding:24px">
      <div class="section-title">Interview Scorecard</div>
      <div class="form-grid">
        <div class="field"><label class="field-label">Candidate Name <span class="req">*</span></label><input type="text" id="t-sc-cand" placeholder="e.g. Sara Ahmed"></div>
        <div class="field"><label class="field-label">Role <span class="req">*</span></label><input type="text" id="t-sc-role" placeholder="e.g. Marketing Manager"></div>
        <div class="field"><label class="field-label">Interview Stage</label><input type="text" id="t-sc-stage" placeholder="e.g. HM Interview"></div>
        <div class="field"><label class="field-label">Interviewer Name</label><input type="text" id="t-sc-interviewer" placeholder="Your name"></div>
        <div class="field full"><label class="field-label">Competencies (one per line with score 1-5)</label>
          <textarea id="t-sc-comps" style="min-height:120px" placeholder="Customer Obsession: 4&#10;Commercial Acumen: 3&#10;Leadership: 5&#10;Communication: 4"></textarea>
        </div>
        <div class="field full"><label class="field-label">Interview Notes / Observations</label><textarea id="t-sc-notes" placeholder="Key observations, examples given, concerns..."></textarea></div>
        <div class="field"><label class="field-label">Overall Recommendation</label>
          <select id="t-sc-rec">
            <option value="Strong Hire">Strong Hire</option>
            <option value="Hire">Hire</option>
            <option value="No Hire">No Hire</option>
            <option value="Strong No Hire">Strong No Hire</option>
          </select>
        </div>
      </div>
      <div class="flex gap-8 mt-20">
        <button class="btn btn-primary" onclick="runScorecardSummary()">Generate AI Summary →</button>
        <button class="btn btn-green btn-sm" onclick="saveScorecard()">Save Scorecard</button>
      </div>
      <div id="scorecard-tool-output" style="display:none">
        <div class="gen-output" id="scorecard-output-text"></div>
        <div class="gen-actions">
          <button class="btn btn-ghost btn-sm" onclick="copyGen('scorecard-output-text')">Copy Summary</button>
        </div>
      </div>
    </div>
  </div>

  <div class="page" id="page-tool-refcheck">
    <div class="table-wrap" style="padding:24px">
      <div class="section-title">Reference Check Generator</div>
      <p style="font-size:12px;color:var(--muted);margin-bottom:20px;line-height:1.7">Generates two emails: one to the candidate requesting references, and one to send to each referee.</p>
      <div class="form-grid">
        <div class="field"><label class="field-label">Candidate Name <span class="req">*</span></label><input type="text" id="t-rc-cand" placeholder="e.g. Sara Ahmed"></div>
        <div class="field"><label class="field-label">Candidate Email</label><input type="email" id="t-rc-cand-email" placeholder="sara@email.com"></div>
        <div class="field"><label class="field-label">Role Title <span class="req">*</span></label><input type="text" id="t-rc-role" placeholder="e.g. Marketing Manager"></div>
        <div class="field"><label class="field-label">Your Name</label><input type="text" id="t-rc-sender" placeholder="Humairaa" value="Humairaa"></div>
        <div class="field"><label class="field-label">Referee 1 Name</label><input type="text" id="t-rc-ref1-name" placeholder="e.g. Ahmed Al Rashid"></div>
        <div class="field"><label class="field-label">Referee 1 Email</label><input type="email" id="t-rc-ref1-email" placeholder="ahmed@company.com"></div>
        <div class="field"><label class="field-label">Referee 2 Name</label><input type="text" id="t-rc-ref2-name" placeholder="e.g. Leila Mansour"></div>
        <div class="field"><label class="field-label">Referee 2 Email</label><input type="email" id="t-rc-ref2-email" placeholder="leila@company.com"></div>
        <div class="field full"><label class="field-label">Key things to probe in reference (optional)</label><textarea id="t-rc-probe" style="min-height:70px" placeholder="e.g. Leadership style, ability to manage ambiguity, commercial impact"></textarea></div>
      </div>
      <div class="flex gap-8 mt-20">
        <button class="btn btn-primary" onclick="runRefCheck()">Generate Reference Emails →</button>
      </div>
      <div id="refcheck-tool-output" style="display:none">
        <!-- Email to candidate -->
        <div class="ref-card mt-20">
          <div class="flex justify-between items-center" style="margin-bottom:10px">
            <div><div class="ref-card-title">Email 1 — To Candidate</div><div class="ref-card-sub">Requesting they provide two references</div></div>
            <button class="btn btn-ghost btn-sm" onclick="openMailRef('ref-cand-email','ref-cand-subject','ref-cand-body')">✉ Open in Mail</button>
          </div>
          <div style="font-size:11px;color:var(--muted);margin-bottom:4px">SUBJECT</div>
          <div id="ref-cand-subject" style="font-weight:600;font-size:13px;margin-bottom:10px"></div>
          <div id="ref-cand-body" class="gen-output" style="margin-top:0;max-height:220px"></div>
        </div>
        <!-- Email to referees -->
        <div class="ref-card">
          <div class="flex justify-between items-center" style="margin-bottom:10px">
            <div><div class="ref-card-title">Email 2 — To Each Referee</div><div class="ref-card-sub">Sent to both referees once the candidate provides details</div></div>
            <button class="btn btn-ghost btn-sm" onclick="openMailRef('ref1-email-addr','ref-ref-subject','ref-ref-body')">✉ Open in Mail</button>
          </div>
          <div style="font-size:11px;color:var(--muted);margin-bottom:4px">SUBJECT</div>
          <div id="ref-ref-subject" style="font-weight:600;font-size:13px;margin-bottom:10px"></div>
          <div id="ref-ref-body" class="gen-output" style="margin-top:0;max-height:220px"></div>
        </div>
        <div class="gen-actions">
          <button class="btn btn-ghost btn-sm" onclick="copyRefEmails()">Copy Both Emails</button>
        </div>
      </div>
      <!-- hidden email stores -->
      <input type="hidden" id="ref-cand-email">
      <input type="hidden" id="ref1-email-addr">
    </div>
  </div>

</div><!-- /content -->

<!-- ── MODALS ── -->

<!-- ADD ROLE -->
<div class="modal-overlay" id="modal-add-role">
  <div class="modal">
    <div class="modal-header">
      <div class="modal-title">New Role</div>
      <div class="modal-close" onclick="closeModal('modal-add-role')">✕</div>
    </div>
    <div class="modal-body">
      <div class="form-grid">
        <div class="field"><label class="field-label">Role Title <span class="req">*</span></label><input type="text" id="m-role-title" placeholder="e.g. Marketing Manager"></div>
        <div class="field"><label class="field-label">Department <span class="req">*</span></label><input type="text" id="m-role-dept" placeholder="e.g. Marketing"></div>
        <div class="field"><label class="field-label">Hiring Manager</label><input type="text" id="m-role-hm" placeholder="e.g. Rami Dandan"></div>
        <div class="field"><label class="field-label">Reports To</label><input type="text" id="m-role-reports" placeholder="e.g. CMO"></div>
        <div class="field"><label class="field-label">Seniority</label>
          <select id="m-role-seniority"><option>Junior</option><option>Mid-level</option><option selected>Senior</option><option>Manager</option><option>Senior Manager</option><option>Head of</option><option>Director</option><option>VP / C-Suite</option></select>
        </div>
        <div class="field"><label class="field-label">Priority</label>
          <select id="m-role-priority"><option>Normal</option><option>High</option><option>Critical</option></select>
        </div>
        <div class="field"><label class="field-label">Location</label><input type="text" id="m-role-location" placeholder="Dubai, UAE" value="Dubai, UAE"></div>
        <div class="field"><label class="field-label">Salary Range</label><input type="text" id="m-role-salary" placeholder="e.g. AED 18-22K/mo"></div>
        <div class="field"><label class="field-label">Status</label>
          <select id="m-role-status"><option value="Open">Open</option><option value="Draft">Draft</option><option value="On Hold">On Hold</option></select>
        </div>
        <div class="field"><label class="field-label">Target Start Date</label><input type="text" id="m-role-start" placeholder="e.g. 1 September 2025"></div>
        <div class="field full"><label class="field-label">Notes</label><textarea id="m-role-notes" style="min-height:60px" placeholder="Any additional context..."></textarea></div>
      </div>
    </div>
    <div class="modal-footer">
      <button class="btn btn-ghost" onclick="closeModal('modal-add-role')">Cancel</button>
      <button class="btn btn-primary" onclick="saveRole()">Create Role</button>
    </div>
  </div>
</div>

<!-- ADD CANDIDATE -->
<div class="modal-overlay" id="modal-add-candidate">
  <div class="modal">
    <div class="modal-header">
      <div class="modal-title">Add Candidate</div>
      <div class="modal-close" onclick="closeModal('modal-add-candidate')">✕</div>
    </div>
    <div class="modal-body">
      <div class="form-grid">
        <div class="field"><label class="field-label">First Name <span class="req">*</span></label><input type="text" id="m-cand-fname" placeholder="Sara"></div>
        <div class="field"><label class="field-label">Last Name <span class="req">*</span></label><input type="text" id="m-cand-lname" placeholder="Ahmed"></div>
        <div class="field"><label class="field-label">Email <span class="req">*</span></label><input type="email" id="m-cand-email" placeholder="sara@email.com"></div>
        <div class="field"><label class="field-label">Phone</label><input type="tel" id="m-cand-phone" placeholder="+971 50 000 0000"></div>
        <div class="field"><label class="field-label">Current Title</label><input type="text" id="m-cand-title" placeholder="e.g. Marketing Lead"></div>
        <div class="field"><label class="field-label">Current Company</label><input type="text" id="m-cand-company" placeholder="e.g. Chalhoub Group"></div>
        <div class="field"><label class="field-label">LinkedIn URL</label><input type="text" id="m-cand-linkedin" placeholder="linkedin.com/in/..."></div>
        <div class="field"><label class="field-label">Source</label>
          <select id="m-cand-source"><option>LinkedIn</option><option>Referral</option><option>Agency</option><option>Direct Apply</option><option>Job Board</option><option>Talent Pool</option></select>
        </div>
        <div class="field"><label class="field-label">Apply to Role</label>
          <select id="m-cand-role"><option value="">— No role yet —</option></select>
        </div>
        <div class="field"><label class="field-label">Starting Stage</label>
          <select id="m-cand-stage"><option>Applied</option><option>Recruiter Screen</option><option>HM Interview</option><option>Task / Case Study</option><option>Panel</option><option>CXO Interview</option><option>Founder Interview</option><option>Offer</option></select>
        </div>
        <div class="field full"><label class="field-label">CV / Notes</label><textarea id="m-cand-notes" placeholder="Paste CV summary, key highlights, or any notes..."></textarea></div>
      </div>
    </div>
    <div class="modal-footer">
      <button class="btn btn-ghost" onclick="closeModal('modal-add-candidate')">Cancel</button>
      <button class="btn btn-primary" onclick="saveCandidate()">Add Candidate</button>
    </div>
  </div>
</div>

<!-- LOADING -->
<div class="loading-overlay" id="loading">
  <div class="loading-spinner"></div>
  <div class="loading-text">GENERATING...</div>
  <div class="loading-sub" id="loading-sub">Claude is working on it</div>
</div>

<!-- TOAST -->
<div class="toast" id="toast"></div>

<script>
// ═══════════════════════════════════════════════
// DATA LAYER (localStorage — AI team replaces with real DB)
// ═══════════════════════════════════════════════
const DB = {
  get: (key) => JSON.parse(localStorage.getItem(`eyewa_ats_${key}`) || '[]'),
  set: (key, val) => localStorage.setItem(`eyewa_ats_${key}`, JSON.stringify(val)),
  id: () => Date.now().toString(36) + Math.random().toString(36).slice(2),
};

const getRoles = () => DB.get('roles');
const getCandidates = () => DB.get('candidates');
const getApplications = () => DB.get('applications');
const getScorecards = () => DB.get('scorecards');
const getComms = () => DB.get('comms');
const getActivity = () => DB.get('activity');

const saveRoles = d => DB.set('roles', d);
const saveCandidates = d => DB.set('candidates', d);
const saveApplications = d => DB.set('applications', d);
const saveScorecards = d => DB.set('scorecards', d);
const saveComms = d => DB.set('comms', d);

function logActivity(text) {
  const acts = getActivity();
  acts.unshift({ text, time: new Date().toISOString() });
  DB.set('activity', acts.slice(0, 20));
}

// ═══════════════════════════════════════════════
// NAV & ROUTING
// ═══════════════════════════════════════════════
let currentPage = 'dashboard';
let currentRoleId = null;
let currentCandidateId = null;

const pageTitles = {
  dashboard: 'DASHBOARD', pipeline: 'PIPELINE', roles: 'ROLES',
  candidates: 'CANDIDATES', 'role-detail': 'ROLE', 'candidate-detail': 'CANDIDATE',
  'tool-jd': 'JD GENERATOR', 'tool-interview': 'INTERVIEW PACK',
  'tool-comms': 'CANDIDATE COMMS', 'tool-scorecard': 'SCORECARDS',
  'tool-refcheck': 'REFERENCE CHECK',
};

function nav(page) {
  document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));
  document.querySelectorAll('.nav-item').forEach(n => n.classList.remove('active'));
  const el = document.getElementById(`page-${page}`);
  if (el) el.classList.add('active');
  document.querySelector(`[onclick="nav('${page}')"]`)?.classList.add('active');
  document.getElementById('topbar-title').textContent = pageTitles[page] || page.toUpperCase();
  currentPage = page;
  if (page === 'dashboard') renderDashboard();
  if (page === 'roles') renderRolesTable();
  if (page === 'candidates') renderCandidatesTable();
  if (page === 'pipeline') renderKanban();
  updateBadges();
}

// ═══════════════════════════════════════════════
// MODALS
// ═══════════════════════════════════════════════
function openModal(id) {
  if (id === 'modal-add-candidate') populateCandidateRoleDropdown();
  document.getElementById(id).classList.add('open');
}
function closeModal(id) {
  document.getElementById(id).classList.remove('open');
}
document.querySelectorAll('.modal-overlay').forEach(m => {
  m.addEventListener('click', e => { if (e.target === m) m.classList.remove('open'); });
});

function populateCandidateRoleDropdown() {
  const sel = document.getElementById('m-cand-role');
  const roles = getRoles().filter(r => r.status === 'Open' || r.status === 'Draft');
  sel.innerHTML = '<option value="">— No role yet —</option>' + roles.map(r => `<option value="${r.id}">${r.title}</option>`).join('');
}

// ═══════════════════════════════════════════════
// ROLES CRUD
// ═══════════════════════════════════════════════
function saveRole() {
  const title = document.getElementById('m-role-title').value.trim();
  const dept = document.getElementById('m-role-dept').value.trim();
  if (!title || !dept) { showToast('Role title and department are required', 'error'); return; }
  const roles = getRoles();
  const role = {
    id: DB.id(), title, dept,
    hm: document.getElementById('m-role-hm').value.trim(),
    reportsTo: document.getElementById('m-role-reports').value.trim(),
    seniority: document.getElementById('m-role-seniority').value,
    priority: document.getElementById('m-role-priority').value,
    location: document.getElementById('m-role-location').value.trim(),
    salary: document.getElementById('m-role-salary').value.trim(),
    status: document.getElementById('m-role-status').value,
    targetStart: document.getElementById('m-role-start').value.trim(),
    notes: document.getElementById('m-role-notes').value.trim(),
    dateOpened: new Date().toISOString().split('T')[0],
    jd: '', interviewPack: '',
  };
  roles.push(role);
  saveRoles(roles);
  logActivity(`New role created: <strong>${title}</strong>`);
  closeModal('modal-add-role');
  clearModalFields('modal-add-role');
  showToast(`Role "${title}" created`);
  updateBadges();
  if (currentPage === 'roles') renderRolesTable();
  if (currentPage === 'dashboard') renderDashboard();
}

function openRoleDetail(id) {
  currentRoleId = id;
  const role = getRoles().find(r => r.id === id);
  if (!role) return;
  document.getElementById('rd-title').textContent = role.title;
  document.getElementById('rd-meta').innerHTML = `
    <span class="badge badge-${role.status === 'Open' ? 'open' : role.status === 'Draft' ? 'draft' : 'hold'}">${role.status}</span>
    <span>${role.dept}</span> · <span>${role.seniority}</span> · <span>${role.location || 'Dubai, UAE'}</span>
    ${role.hm ? `· <span>HM: ${role.hm}</span>` : ''}
  `;
  // Overview grid
  document.getElementById('rd-overview-grid').innerHTML = `
    <div class="field"><div class="field-label">Department</div><div class="info-val">${role.dept}</div></div>
    <div class="field"><div class="field-label">Hiring Manager</div><div class="info-val">${role.hm || '—'}</div></div>
    <div class="field"><div class="field-label">Reports To</div><div class="info-val">${role.reportsTo || '—'}</div></div>
    <div class="field"><div class="field-label">Seniority</div><div class="info-val">${role.seniority}</div></div>
    <div class="field"><div class="field-label">Priority</div><div class="info-val">${role.priority}</div></div>
    <div class="field"><div class="field-label">Location</div><div class="info-val">${role.location || 'Dubai, UAE'}</div></div>
    <div class="field"><div class="field-label">Salary Range</div><div class="info-val">${role.salary || '—'}</div></div>
    <div class="field"><div class="field-label">Target Start</div><div class="info-val">${role.targetStart || '—'}</div></div>
    <div class="field"><div class="field-label">Date Opened</div><div class="info-val">${role.dateOpened}</div></div>
    <div class="field"><div class="field-label">Status</div><div class="info-val">${role.status}</div></div>
    ${role.notes ? `<div class="field" style="grid-column:1/-1"><div class="field-label">Notes</div><div class="info-val" style="font-size:13px;color:var(--mid);line-height:1.6">${role.notes}</div></div>` : ''}
  `;
  // Candidates
  renderRoleCandidates(id);
  // JD
  const jdDiv = document.getElementById('rd-jd-content');
  jdDiv.innerHTML = role.jd
    ? `<div class="gen-output">${role.jd}</div><div class="gen-actions"><button class="btn btn-ghost btn-sm" onclick="copyGen('rd-jd-raw')">Copy</button><button class="btn btn-primary btn-sm" onclick="regenerateJDForRole()">Regenerate</button></div><div id="rd-jd-raw" style="display:none">${role.jd}</div>`
    : `<div class="empty"><div class="empty-icon">✦</div><div class="empty-title">No JD yet</div><div class="empty-sub">Click "Generate Tools" to auto-generate a JD for this role</div></div><button class="btn btn-primary btn-sm mt-12" onclick="prefillAndGoJD()">Generate JD →</button>`;
  // Interview Pack
  const ipDiv = document.getElementById('rd-interview-content');
  ipDiv.innerHTML = role.interviewPack
    ? `<div class="gen-output">${role.interviewPack}</div><div class="gen-actions"><button class="btn btn-ghost btn-sm" onclick="copyGen('rd-ip-raw')">Copy</button><button class="btn btn-primary btn-sm" onclick="regenerateIPForRole()">Regenerate</button></div><div id="rd-ip-raw" style="display:none">${role.interviewPack}</div>`
    : `<div class="empty"><div class="empty-icon">◈</div><div class="empty-title">No interview pack yet</div></div><button class="btn btn-primary btn-sm mt-12" onclick="prefillAndGoInterview()">Generate Interview Pack →</button>`;

  // Reset tabs
  document.querySelectorAll('#page-role-detail .tab').forEach(t => t.classList.remove('active'));
  document.querySelectorAll('#page-role-detail .tab-panel').forEach(t => t.classList.remove('active'));
  document.querySelector('#page-role-detail .tab').classList.add('active');
  document.getElementById('role-tab-overview').classList.add('active');
  nav('role-detail');
}

function renderRoleCandidates(roleId) {
  const apps = getApplications().filter(a => a.roleId === roleId);
  const cands = getCandidates();
  const el = document.getElementById('rd-candidates-list');
  if (!apps.length) {
    el.innerHTML = `<div class="empty"><div class="empty-icon">◯</div><div class="empty-title">No candidates yet</div><div class="empty-sub">Add a candidate and tag them to this role</div></div><button class="btn btn-primary btn-sm mt-12" onclick="openAddCandidateToRole()">+ Add Candidate</button>`;
    return;
  }
  el.innerHTML = `<div class="table-wrap"><table>
    <thead><tr><th>Name</th><th>Stage</th><th>Source</th><th>Days in Stage</th><th>Actions</th></tr></thead>
    <tbody>${apps.map(app => {
      const c = cands.find(x => x.id === app.candidateId);
      if (!c) return '';
      const days = Math.floor((Date.now() - new Date(app.stageUpdated)) / 86400000);
      return `<tr onclick="openCandidateDetail('${c.id}')">
        <td><strong>${c.firstName} ${c.lastName}</strong></td>
        <td><span class="badge badge-active">${app.stage}</span></td>
        <td><span class="text-muted text-sm">${c.source}</span></td>
        <td><span class="text-muted text-sm">${days}d</span></td>
        <td><button class="btn btn-ghost btn-sm" onclick="event.stopPropagation();moveStage('${app.id}')">Move Stage</button></td>
      </tr>`;
    }).join('')}</tbody>
  </table></div>`;
}

function openAddCandidateToRole() {
  openModal('modal-add-candidate');
  if (currentRoleId) {
    populateCandidateRoleDropdown();
    setTimeout(() => {
      document.getElementById('m-cand-role').value = currentRoleId;
    }, 50);
  }
}

function openRoleTools() {
  const role = getRoles().find(r => r.id === currentRoleId);
  if (!role) return;
  prefillAndGoJD();
}

function prefillAndGoJD() {
  const role = getRoles().find(r => r.id === currentRoleId);
  if (!role) return;
  document.getElementById('t-jd-title').value = role.title;
  document.getElementById('t-jd-dept').value = role.dept;
  document.getElementById('t-jd-reports').value = role.reportsTo || '';
  nav('tool-jd');
}

function prefillAndGoInterview() {
  const role = getRoles().find(r => r.id === currentRoleId);
  if (!role) return;
  document.getElementById('t-ip-title').value = role.title;
  nav('tool-interview');
}

// ═══════════════════════════════════════════════
// CANDIDATES CRUD
// ═══════════════════════════════════════════════
function saveCandidate() {
  const fn = document.getElementById('m-cand-fname').value.trim();
  const ln = document.getElementById('m-cand-lname').value.trim();
  const email = document.getElementById('m-cand-email').value.trim();
  if (!fn || !ln || !email) { showToast('Name and email are required', 'error'); return; }
  const cands = getCandidates();
  const cand = {
    id: DB.id(), firstName: fn, lastName: ln, email,
    phone: document.getElementById('m-cand-phone').value.trim(),
    currentTitle: document.getElementById('m-cand-title').value.trim(),
    currentCompany: document.getElementById('m-cand-company').value.trim(),
    linkedin: document.getElementById('m-cand-linkedin').value.trim(),
    source: document.getElementById('m-cand-source').value,
    notes: document.getElementById('m-cand-notes').value.trim(),
    dateAdded: new Date().toISOString().split('T')[0],
  };
  cands.push(cand);
  saveCandidates(cands);
  // Create application if role selected
  const roleId = document.getElementById('m-cand-role').value;
  const stage = document.getElementById('m-cand-stage').value;
  if (roleId) {
    const apps = getApplications();
    apps.push({
      id: DB.id(), candidateId: cand.id, roleId,
      stage, stageUpdated: new Date().toISOString(),
      status: 'Active',
    });
    saveApplications(apps);
    const role = getRoles().find(r => r.id === roleId);
    logActivity(`<strong>${fn} ${ln}</strong> added to <strong>${role?.title || 'role'}</strong> — ${stage}`);
  } else {
    logActivity(`New candidate added: <strong>${fn} ${ln}</strong>`);
  }
  closeModal('modal-add-candidate');
  clearModalFields('modal-add-candidate');
  showToast(`${fn} ${ln} added`);
  updateBadges();
  if (currentPage === 'candidates') renderCandidatesTable();
  if (currentPage === 'role-detail') renderRoleCandidates(currentRoleId);
  if (currentPage === 'dashboard') renderDashboard();
}

function openCandidateDetail(id) {
  currentCandidateId = id;
  const cand = getCandidates().find(c => c.id === id);
  if (!cand) return;
  const apps = getApplications().filter(a => a.candidateId === id);
  const roles = getRoles();
  document.getElementById('cd-name').textContent = `${cand.firstName} ${cand.lastName}`;
  const appRole = apps.length ? roles.find(r => r.id === apps[apps.length-1].roleId) : null;
  document.getElementById('cd-meta').innerHTML = `
    ${cand.currentTitle ? `<span>${cand.currentTitle}</span> ·` : ''}
    ${cand.currentCompany ? `<span>${cand.currentCompany}</span> ·` : ''}
    <span>${cand.email}</span>
    ${appRole ? `· <span class="badge badge-active">${apps[apps.length-1]?.stage}</span>` : ''}
  `;
  // Overview
  document.getElementById('cd-overview-grid').innerHTML = `
    <div class="field"><div class="field-label">Email</div><div class="info-val">${cand.email}</div></div>
    <div class="field"><div class="field-label">Phone</div><div class="info-val">${cand.phone || '—'}</div></div>
    <div class="field"><div class="field-label">Current Title</div><div class="info-val">${cand.currentTitle || '—'}</div></div>
    <div class="field"><div class="field-label">Current Company</div><div class="info-val">${cand.currentCompany || '—'}</div></div>
    <div class="field"><div class="field-label">Source</div><div class="info-val">${cand.source}</div></div>
    <div class="field"><div class="field-label">Date Added</div><div class="info-val">${cand.dateAdded}</div></div>
    ${cand.linkedin ? `<div class="field"><div class="field-label">LinkedIn</div><div class="info-val"><a href="${cand.linkedin}" target="_blank" style="color:var(--accent2)">${cand.linkedin}</a></div></div>` : ''}
    ${apps.length ? `<div class="field"><div class="field-label">Applied To</div><div class="info-val">${apps.map(a => { const r = roles.find(x => x.id === a.roleId); return r ? r.title : ''; }).filter(Boolean).join(', ')}</div></div>` : ''}
    ${cand.notes ? `<div class="field" style="grid-column:1/-1"><div class="field-label">Notes / CV Summary</div><div class="info-val" style="font-size:13px;color:var(--mid);line-height:1.6;white-space:pre-wrap">${cand.notes}</div></div>` : ''}
  `;
  // Comms
  renderCandidateComms(id);
  // Scorecards
  renderCandidateScorecards(id);
  // Reset tabs
  document.querySelectorAll('#page-candidate-detail .tab').forEach(t => t.classList.remove('active'));
  document.querySelectorAll('#page-candidate-detail .tab-panel').forEach(t => t.classList.remove('active'));
  document.querySelector('#page-candidate-detail .tab').classList.add('active');
  document.getElementById('candidate-tab-overview').classList.add('active');
  nav('candidate-detail');
}

function renderCandidateComms(candidateId) {
  const apps = getApplications().filter(a => a.candidateId === candidateId);
  const appIds = apps.map(a => a.id);
  const comms = getComms().filter(c => appIds.includes(c.applicationId) || c.candidateId === candidateId);
  const el = document.getElementById('cd-comms-list');
  if (!comms.length) {
    el.innerHTML = `<div class="empty"><div class="empty-icon">✉</div><div class="empty-title">No comms yet</div></div><button class="btn btn-primary btn-sm mt-12" onclick="openCandidateComms()">Send First Email →</button>`;
    return;
  }
  el.innerHTML = comms.map(c => `
    <div class="comms-item">
      <div class="comms-item-header"><span class="comms-type">${c.type}</span><span class="comms-date">${c.date}</span></div>
      <div class="comms-subject">${c.subject}</div>
      <div class="comms-preview">${c.body?.slice(0, 140)}...</div>
    </div>
  `).join('');
}

function renderCandidateScorecards(candidateId) {
  const apps = getApplications().filter(a => a.candidateId === candidateId);
  const appIds = apps.map(a => a.id);
  const scs = getScorecards().filter(s => appIds.includes(s.applicationId) || s.candidateId === candidateId);
  const el = document.getElementById('cd-scorecards-list');
  if (!scs.length) {
    el.innerHTML = `<div class="empty"><div class="empty-icon">◉</div><div class="empty-title">No scorecards yet</div></div><button class="btn btn-primary btn-sm mt-12" onclick="openScorecard()">Submit Scorecard →</button>`;
    return;
  }
  el.innerHTML = scs.map(sc => `
    <div class="ref-card">
      <div class="flex justify-between items-center" style="margin-bottom:8px">
        <div class="ref-card-title">${sc.stage} — ${sc.interviewer}</div>
        <span class="badge ${sc.recommendation === 'Strong Hire' || sc.recommendation === 'Hire' ? 'badge-hired' : 'badge-rejected'}">${sc.recommendation}</span>
      </div>
      <div class="ref-card-sub" style="margin-bottom:10px">${sc.date}</div>
      ${sc.aiSummary ? `<div style="font-size:12px;color:var(--mid);line-height:1.6;white-space:pre-wrap">${sc.aiSummary}</div>` : ''}
    </div>
  `).join('');
}

function openCandidateComms() {
  const cand = getCandidates().find(c => c.id === currentCandidateId);
  const apps = getApplications().filter(a => a.candidateId === currentCandidateId);
  const role = apps.length ? getRoles().find(r => r.id === apps[apps.length-1].roleId) : null;
  if (cand) {
    document.getElementById('t-cm-name').value = cand.firstName;
    document.getElementById('t-cm-email').value = cand.email;
    document.getElementById('t-cm-role').value = role?.title || '';
  }
  nav('tool-comms');
}

function openScorecard() {
  const cand = getCandidates().find(c => c.id === currentCandidateId);
  const apps = getApplications().filter(a => a.candidateId === currentCandidateId);
  const role = apps.length ? getRoles().find(r => r.id === apps[apps.length-1].roleId) : null;
  if (cand) {
    document.getElementById('t-sc-cand').value = `${cand.firstName} ${cand.lastName}`;
    document.getElementById('t-sc-role').value = role?.title || '';
  }
  nav('tool-scorecard');
}

function openRefCheck() {
  const cand = getCandidates().find(c => c.id === currentCandidateId);
  const apps = getApplications().filter(a => a.candidateId === currentCandidateId);
  const role = apps.length ? getRoles().find(r => r.id === apps[apps.length-1].roleId) : null;
  if (cand) {
    document.getElementById('t-rc-cand').value = `${cand.firstName} ${cand.lastName}`;
    document.getElementById('t-rc-cand-email').value = cand.email;
    document.getElementById('t-rc-role').value = role?.title || '';
  }
  nav('tool-refcheck');
}

// ═══════════════════════════════════════════════
// RENDER TABLES
// ═══════════════════════════════════════════════
function renderRolesTable() {
  const roles = getRoles();
  const tbody = document.getElementById('roles-tbody');
  document.getElementById('roles-empty').style.display = roles.length ? 'none' : 'block';
  const apps = getApplications();
  tbody.innerHTML = roles.map(r => {
    const count = apps.filter(a => a.roleId === r.id).length;
    const pClass = r.priority === 'Critical' ? 'p-critical' : r.priority === 'High' ? 'p-high' : 'p-normal';
    const sClass = r.status === 'Open' ? 'badge-open' : r.status === 'Draft' ? 'badge-draft' : 'badge-hold';
    return `<tr onclick="openRoleDetail('${r.id}')">
      <td><strong>${r.title}</strong></td>
      <td>${r.dept}</td>
      <td><span class="priority-dot ${pClass}"></span>${r.priority}</td>
      <td><span class="badge ${sClass}">${r.status}</span></td>
      <td>${count}</td>
      <td class="text-muted text-sm">${r.dateOpened}</td>
      <td><button class="btn btn-ghost btn-sm" onclick="event.stopPropagation();openRoleDetail('${r.id}')">Open →</button></td>
    </tr>`;
  }).join('');
}

function renderCandidatesTable() {
  const cands = getCandidates();
  const tbody = document.getElementById('candidates-tbody');
  document.getElementById('candidates-empty').style.display = cands.length ? 'none' : 'block';
  const apps = getApplications();
  const roles = getRoles();
  tbody.innerHTML = cands.map(c => {
    const app = apps.filter(a => a.candidateId === c.id).pop();
    const role = app ? roles.find(r => r.id === app.roleId) : null;
    const stageClass = app ? (app.stage === 'Offer' ? 'badge-offer' : app.stage === 'Hired' ? 'badge-hired' : app.stage === 'Rejected' ? 'badge-rejected' : 'badge-active') : 'badge-draft';
    return `<tr onclick="openCandidateDetail('${c.id}')">
      <td><strong>${c.firstName} ${c.lastName}</strong><div class="text-muted text-sm">${c.email}</div></td>
      <td>${c.currentTitle ? `${c.currentTitle}${c.currentCompany ? ' · ' + c.currentCompany : ''}` : '—'}</td>
      <td><span class="text-muted text-sm">${c.source}</span></td>
      <td>${role ? role.title : '<span class="text-muted">—</span>'}</td>
      <td>${app ? `<span class="badge ${stageClass}">${app.stage}</span>` : '—'}</td>
      <td class="text-muted text-sm">${c.dateAdded}</td>
      <td><button class="btn btn-ghost btn-sm" onclick="event.stopPropagation();openCandidateDetail('${c.id}')">View →</button></td>
    </tr>`;
  }).join('');
}

// ═══════════════════════════════════════════════
// KANBAN
// ═══════════════════════════════════════════════
const STAGES = ['Applied','Recruiter Screen','HM Interview','Task / Case Study','Panel','CXO Interview','Founder Interview','Offer','Hired'];
const STAGE_COLORS = { 'Applied':'var(--muted)','Recruiter Screen':'var(--accent)','HM Interview':'var(--accent2)','Task / Case Study':'var(--purple)','Panel':'var(--orange)','CXO Interview':'var(--red)','Founder Interview':'#e3b341','Offer':'var(--green)','Hired':'var(--green)' };

function renderKanban() {
  const apps = getApplications();
  const cands = getCandidates();
  const roles = getRoles();
  const board = document.getElementById('kanban-board');
  board.innerHTML = STAGES.map(stage => {
    const stageApps = apps.filter(a => a.stage === stage && a.status === 'Active');
    const cards = stageApps.map(app => {
      const c = cands.find(x => x.id === app.candidateId);
      const r = roles.find(x => x.id === app.roleId);
      if (!c) return '';
      const days = Math.floor((Date.now() - new Date(app.stageUpdated)) / 86400000);
      return `<div class="kanban-card" onclick="openCandidateDetail('${c.id}')">
        <div class="kc-name">${c.firstName} ${c.lastName}</div>
        <div class="kc-role">${r?.title || '—'}</div>
        <div class="kc-meta">
          <span class="kc-days">${days}d in stage</span>
          <span class="text-muted text-sm">${c.source}</span>
        </div>
      </div>`;
    }).join('');
    return `<div class="kanban-col">
      <div class="kanban-col-header">
        <div class="kanban-col-name" style="color:${STAGE_COLORS[stage]}">${stage}</div>
        <div class="kanban-col-count">${stageApps.length}</div>
      </div>
      <div class="kanban-cards">${cards || `<div class="text-muted text-sm" style="padding:8px;text-align:center">Empty</div>`}</div>
    </div>`;
  }).join('');
}

function moveStage(appId) {
  const apps = getApplications();
  const app = apps.find(a => a.id === appId);
  if (!app) return;
  const idx = STAGES.indexOf(app.stage);
  if (idx < STAGES.length - 1) {
    app.stage = STAGES[idx + 1];
    app.stageUpdated = new Date().toISOString();
    saveApplications(apps);
    const cand = getCandidates().find(c => c.id === app.candidateId);
    logActivity(`<strong>${cand?.firstName} ${cand?.lastName}</strong> moved to <strong>${app.stage}</strong>`);
    showToast(`Moved to ${app.stage}`);
    renderRoleCandidates(currentRoleId);
  }
}

// ═══════════════════════════════════════════════
// DASHBOARD
// ═══════════════════════════════════════════════
function renderDashboard() {
  const roles = getRoles();
  const apps = getApplications();
  const scs = getScorecards();
  const open = roles.filter(r => r.status === 'Open').length;
  const active = apps.filter(a => a.status === 'Active' && !['Hired','Rejected','Withdrawn'].includes(a.stage)).length;
  const hired = apps.filter(a => a.stage === 'Hired').length;
  const pendingFeedback = apps.filter(a => a.status === 'Active' && a.stage.includes('Interview') && !scs.find(s => s.applicationId === a.id)).length;
  document.getElementById('stat-open').textContent = open;
  document.getElementById('stat-candidates').textContent = active;
  document.getElementById('stat-hired').textContent = hired;
  document.getElementById('stat-feedback').textContent = pendingFeedback;
  // Open roles list
  const el = document.getElementById('dash-roles-list');
  const openRoles = roles.filter(r => r.status === 'Open').slice(0, 6);
  el.innerHTML = openRoles.length ? openRoles.map(r => {
    const count = apps.filter(a => a.roleId === r.id).length;
    const pClass = r.priority === 'Critical' ? 'p-critical' : r.priority === 'High' ? 'p-high' : 'p-normal';
    return `<div class="activity-item" style="cursor:pointer" onclick="openRoleDetail('${r.id}')">
      <div class="priority-dot ${pClass}"></div>
      <div><div style="font-size:13px;font-weight:600">${r.title}</div><div class="text-muted text-sm">${r.dept} · ${count} candidate${count !== 1 ? 's' : ''}</div></div>
    </div>`;
  }).join('') : `<div class="empty"><div class="empty-icon">◻</div><div class="empty-title">No open roles</div></div>`;
  // Activity
  const acts = getActivity();
  const actEl = document.getElementById('activity-feed');
  actEl.innerHTML = acts.length ? acts.slice(0, 8).map(a => `
    <div class="activity-item">
      <div class="activity-dot"></div>
      <div><div class="activity-text">${a.text}</div><div class="activity-time">${new Date(a.time).toLocaleDateString('en-GB', {day:'numeric',month:'short',hour:'2-digit',minute:'2-digit'})}</div></div>
    </div>
  `).join('') : `<div class="empty"><div class="empty-icon">◈</div><div class="empty-title">No activity yet</div></div>`;
}

// ═══════════════════════════════════════════════
// BADGES
// ═══════════════════════════════════════════════
function updateBadges() {
  document.getElementById('nb-roles').textContent = getRoles().filter(r => r.status === 'Open').length;
  document.getElementById('nb-candidates').textContent = getCandidates().length;
  document.getElementById('nb-pipeline').textContent = getApplications().filter(a => a.status === 'Active').length;
}

// ═══════════════════════════════════════════════
// AI GENERATORS
// ═══════════════════════════════════════════════
const PROXY = 'https://api.anthropic.com/v1/messages';

async function callClaude(prompt, maxTokens = 2000) {
  const res = await fetch(PROXY, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json', 'x-api-key': '', 'anthropic-version': '2023-06-01' },
    body: JSON.stringify({ model: 'claude-sonnet-4-20250514', max_tokens: maxTokens, messages: [{ role: 'user', content: prompt }] })
  });
  const data = await res.json();
  if (data.error) throw new Error(data.error.message);
  return data.content.map(b => b.text || '').join('');
}

async function runJDGenerator() {
  const title = document.getElementById('t-jd-title').value.trim();
  const resp = document.getElementById('t-jd-resp').value.trim();
  const req = document.getElementById('t-jd-req').value.trim();
  if (!title || !resp || !req) { showToast('Fill in title, responsibilities and requirements', 'error'); return; }
  showLoading('Writing your JD...');
  try {
    const text = await callClaude(`You are an employer brand copywriter for Eyewa, the Middle East's leading eyewear retailer. Write a complete job description.

ROLE: ${title} | DEPT: ${document.getElementById('t-jd-dept').value} | REPORTS TO: ${document.getElementById('t-jd-reports').value} | LEVEL: ${document.getElementById('t-jd-level').value} | TONE: ${document.getElementById('t-jd-tone').value}

RESPONSIBILITIES: ${resp}
REQUIREMENTS: ${req}

Write in Eyewa format:
ABOUT EYEWA
[Always use: "Eyewa is the Middle East's leading eyewear destination, offering the widest selection of eyeglasses, sunglasses, and contact lenses across the GCC. Founded in 2017, we've grown from a digital-first startup to a multi-channel retailer with over 100 stores and millions of customers served. We move fast, think big, and obsess over our customers."]

OVERVIEW
[2-3 compelling sentences]

WHAT YOU'LL DO
[7-8 bullet points starting with action verbs]

WHAT WE'RE LOOKING FOR
[6-7 bullet points, specific requirements]

WHAT WE OFFER
[5-6 bullet points — real benefits, fast-growth environment, ownership, development]`);

    document.getElementById('jd-output-text').textContent = text;
    document.getElementById('jd-tool-output').style.display = 'block';
    // Save to role if open
    if (currentRoleId) {
      const roles = getRoles();
      const role = roles.find(r => r.id === currentRoleId);
      if (role) { role.jd = text; saveRoles(roles); }
    }
    logActivity(`JD generated: <strong>${title}</strong>`);
  } catch(e) { showToast(e.message, 'error'); }
  hideLoading();
}

async function runInterviewPack() {
  const title = document.getElementById('t-ip-title').value.trim();
  const jd = document.getElementById('t-ip-jd').value.trim();
  if (!title || !jd) { showToast('Fill in role title and JD', 'error'); return; }
  showLoading('Building interview pack...');
  try {
    const text = await callClaude(`You are an expert TA specialist at Eyewa, a leading GCC eyewear retailer. Generate a complete interview question pack.

ROLE: ${title} | LEVEL: ${document.getElementById('t-ip-level').value}
STAGES: ${document.getElementById('t-ip-stages').value}
COMPETENCIES TO FOCUS ON: ${document.getElementById('t-ip-comps').value || 'Derive from JD'}
JD / CONTEXT: ${jd}

For each stage, generate:
- Stage name and purpose (1 sentence)
- 3-4 competency sections, each with:
  • Main question (behavioural — "Tell me about a time...")
  • Follow-up probe
  • Red flag to watch for

Also include a SCORECARD TEMPLATE at the end with all competencies, a 1-5 scale, and "what great looks like" for each.

Make every question SPECIFIC to this role — no generic questions.`, 3000);

    document.getElementById('interview-output-text').textContent = text;
    document.getElementById('interview-tool-output').style.display = 'block';
    if (currentRoleId) {
      const roles = getRoles();
      const role = roles.find(r => r.id === currentRoleId);
      if (role) { role.interviewPack = text; saveRoles(roles); }
    }
    logActivity(`Interview pack generated: <strong>${title}</strong>`);
  } catch(e) { showToast(e.message, 'error'); }
  hideLoading();
}

let lastCommsData = {};
async function runCommsGenerator() {
  const name = document.getElementById('t-cm-name').value.trim();
  const role = document.getElementById('t-cm-role').value.trim();
  const type = document.getElementById('t-cm-type').value;
  const sender = document.getElementById('t-cm-sender').value.trim();
  const context = document.getElementById('t-cm-context').value.trim();
  if (!name || !role) { showToast('Candidate name and role required', 'error'); return; }
  showLoading('Writing email...');
  try {
    const text = await callClaude(`You are ${sender}, TA at Eyewa. Write a warm, professional candidate email. Never use "I hope this email finds you well". Never sound robotic.

EMAIL TYPE: ${type}
CANDIDATE: ${name}
ROLE: ${role}
CONTEXT: ${context || 'None'}
SENDER: ${sender}, Talent Acquisition, Eyewa

Return ONLY valid JSON: {"subject":"...","body":"full email body with \\n for line breaks"}`, 800);

    const clean = text.replace(/```json\n?/g,'').replace(/```\n?/g,'').trim();
    const parsed = JSON.parse(clean);
    lastCommsData = { ...parsed, to: document.getElementById('t-cm-email').value.trim(), type, candidateId: currentCandidateId };
    document.getElementById('comms-output-subject').textContent = parsed.subject;
    document.getElementById('comms-output-body').textContent = parsed.body;
    document.getElementById('comms-tool-output').style.display = 'block';
    // Log to comms history
    if (currentCandidateId) {
      const apps = getApplications().filter(a => a.candidateId === currentCandidateId);
      const comms = getComms();
      comms.push({ id: DB.id(), candidateId: currentCandidateId, applicationId: apps[apps.length-1]?.id, type, subject: parsed.subject, body: parsed.body, date: new Date().toLocaleDateString('en-GB'), sender });
      saveComms(comms);
      logActivity(`Email sent to <strong>${name}</strong>: ${type}`);
    }
  } catch(e) { showToast(e.message, 'error'); }
  hideLoading();
}

function openMailFromComms() {
  const to = document.getElementById('t-cm-email').value.trim();
  const subject = document.getElementById('comms-output-subject').textContent;
  const body = document.getElementById('comms-output-body').textContent;
  window.location.href = `mailto:${encodeURIComponent(to)}?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`;
}

async function runScorecardSummary() {
  const cand = document.getElementById('t-sc-cand').value.trim();
  const role = document.getElementById('t-sc-role').value.trim();
  const comps = document.getElementById('t-sc-comps').value.trim();
  const notes = document.getElementById('t-sc-notes').value.trim();
  const rec = document.getElementById('t-sc-rec').value;
  if (!cand || !role || !comps) { showToast('Fill in candidate, role and competency scores', 'error'); return; }
  showLoading('Generating AI summary...');
  try {
    const text = await callClaude(`You are a senior TA partner at Eyewa. Summarise this interview scorecard into a clear hire/no-hire recommendation.

CANDIDATE: ${cand} | ROLE: ${role} | STAGE: ${document.getElementById('t-sc-stage').value} | INTERVIEWER: ${document.getElementById('t-sc-interviewer').value}
RECOMMENDATION: ${rec}
COMPETENCY SCORES:
${comps}
INTERVIEW NOTES: ${notes || 'None provided'}

Write:
1. HEADLINE: One sentence verdict
2. STRENGTHS: 2-3 specific strengths observed
3. RISKS / GAPS: 1-2 honest concerns (or "None identified")
4. RECOMMENDATION: Clear hire/no hire rationale in 2-3 sentences

Be direct and specific. This is for the hiring manager to read.`);

    document.getElementById('scorecard-output-text').textContent = text;
    document.getElementById('scorecard-tool-output').style.display = 'block';
    logActivity(`Scorecard submitted: <strong>${cand}</strong> — ${rec}`);
  } catch(e) { showToast(e.message, 'error'); }
  hideLoading();
}

function saveScorecard() {
  const cand = document.getElementById('t-sc-cand').value.trim();
  const role = document.getElementById('t-sc-role').value.trim();
  if (!cand || !role) { showToast('Fill in candidate and role first', 'error'); return; }
  const apps = currentCandidateId ? getApplications().filter(a => a.candidateId === currentCandidateId) : [];
  const scs = getScorecards();
  scs.push({
    id: DB.id(),
    candidateId: currentCandidateId,
    applicationId: apps[apps.length-1]?.id,
    stage: document.getElementById('t-sc-stage').value,
    interviewer: document.getElementById('t-sc-interviewer').value,
    competencies: document.getElementById('t-sc-comps').value,
    notes: document.getElementById('t-sc-notes').value,
    recommendation: document.getElementById('t-sc-rec').value,
    aiSummary: document.getElementById('scorecard-output-text').textContent,
    date: new Date().toLocaleDateString('en-GB'),
  });
  saveScorecards(scs);
  showToast('Scorecard saved');
  logActivity(`Scorecard saved for <strong>${cand}</strong>`);
}

let lastRefData = {};
async function runRefCheck() {
  const cand = document.getElementById('t-rc-cand').value.trim();
  const role = document.getElementById('t-rc-role').value.trim();
  const sender = document.getElementById('t-rc-sender').value.trim();
  const probe = document.getElementById('t-rc-probe').value.trim();
  const ref1Name = document.getElementById('t-rc-ref1-name').value.trim();
  const ref2Name = document.getElementById('t-rc-ref2-name').value.trim();
  if (!cand || !role) { showToast('Candidate name and role required', 'error'); return; }
  showLoading('Generating reference check emails...');
  try {
    const text = await callClaude(`You are ${sender}, TA at Eyewa. Generate two reference check emails.

CANDIDATE: ${cand}
ROLE: ${role}
REFEREE 1: ${ref1Name || 'Reference 1'}
REFEREE 2: ${ref2Name || 'Reference 2'}
KEY AREAS TO PROBE: ${probe || 'General performance, working style, strengths and development areas'}
SENDER: ${sender}, Talent Acquisition, Eyewa

Return ONLY valid JSON:
{
  "candEmail": {
    "subject": "...",
    "body": "Email to candidate asking them to provide two professional references. Warm, clear. Explain we need: full name, job title, company, relationship to candidate, email address. Give them 2 working days. Sign off as ${sender}."
  },
  "refEmail": {
    "subject": "...",
    "body": "Email to referee. Professional, warm. Introduce yourself and Eyewa. Explain ${cand} has applied for ${role}. Ask 5-6 specific reference questions covering: overall performance, key strengths, areas for development, leadership/collaboration style, ${probe ? 'specifically: ' + probe + ',' : ''} and whether they'd rehire. Keep it to a 10-15 min response. Thank them. Sign off as ${sender}."
  }
}`, 1200);

    const clean = text.replace(/```json\n?/g,'').replace(/```\n?/g,'').trim();
    const parsed = JSON.parse(clean);
    lastRefData = parsed;
    document.getElementById('ref-cand-subject').textContent = parsed.candEmail.subject;
    document.getElementById('ref-cand-body').textContent = parsed.candEmail.body;
    document.getElementById('ref-ref-subject').textContent = parsed.refEmail.subject;
    document.getElementById('ref-ref-body').textContent = parsed.refEmail.body;
    document.getElementById('ref-cand-email').value = document.getElementById('t-rc-cand-email').value.trim();
    document.getElementById('ref1-email-addr').value = document.getElementById('t-rc-ref1-email').value.trim();
    document.getElementById('refcheck-tool-output').style.display = 'block';
    logActivity(`Reference check emails generated for <strong>${cand}</strong>`);
  } catch(e) { showToast(e.message, 'error'); }
  hideLoading();
}

function openMailRef(emailField, subjectId, bodyId) {
  const to = document.getElementById(emailField)?.value || '';
  const subject = document.getElementById(subjectId).textContent;
  const body = document.getElementById(bodyId).textContent;
  window.location.href = `mailto:${encodeURIComponent(to)}?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`;
}

function copyRefEmails() {
  const s1 = document.getElementById('ref-cand-subject').textContent;
  const b1 = document.getElementById('ref-cand-body').textContent;
  const s2 = document.getElementById('ref-ref-subject').textContent;
  const b2 = document.getElementById('ref-ref-body').textContent;
  navigator.clipboard.writeText(`EMAIL 1 — TO CANDIDATE\nSubject: ${s1}\n\n${b1}\n\n---\n\nEMAIL 2 — TO REFEREE\nSubject: ${s2}\n\n${b2}`);
  showToast('Both emails copied');
}

// ═══════════════════════════════════════════════
// UTILITIES
// ═══════════════════════════════════════════════
function switchTab(context, tab, el) {
  const prefix = context === 'role' ? 'role-tab-' : 'candidate-tab-';
  const pageId = `page-${context}-detail`;
  document.querySelectorAll(`#${pageId} .tab`).forEach(t => t.classList.remove('active'));
  document.querySelectorAll(`#${pageId} .tab-panel`).forEach(p => p.classList.remove('active'));
  el.classList.add('active');
  document.getElementById(`${prefix}${tab}`)?.classList.add('active');
}

function filterTable(tableId, query) {
  const q = query.toLowerCase();
  document.querySelectorAll(`#${tableId} tbody tr`).forEach(tr => {
    tr.style.display = tr.textContent.toLowerCase().includes(q) ? '' : 'none';
  });
}

function copyGen(id) {
  const el = document.getElementById(id);
  navigator.clipboard.writeText(el.textContent);
  showToast('Copied to clipboard');
}

function clearTool(tool) {
  document.querySelectorAll(`#page-tool-${tool} input, #page-tool-${tool} textarea`).forEach(el => {
    if (!el.classList.contains('keep')) el.value = '';
  });
}

function clearModalFields(modalId) {
  document.querySelectorAll(`#${modalId} input, #${modalId} textarea`).forEach(el => {
    if (el.type !== 'submit') el.value = '';
  });
}

function showLoading(sub) {
  document.getElementById('loading').classList.add('on');
  document.getElementById('loading-sub').textContent = sub || 'Claude is working on it';
}
function hideLoading() { document.getElementById('loading').classList.remove('on'); }

function showToast(msg, type) {
  const t = document.getElementById('toast');
  t.textContent = msg;
  t.className = `toast${type === 'error' ? ' error' : ''}`;
  t.classList.add('show');
  setTimeout(() => t.classList.remove('show'), 3000);
}

// ═══════════════════════════════════════════════
// INIT
// ═══════════════════════════════════════════════
renderDashboard();
updateBadges();
</script>
</body>
</html>
